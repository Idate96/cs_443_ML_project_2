1 run preprocess-twitter.rb on all original dataset from terminal
Mac OS: ruby -n preprocess-twitter.rb train_neg_full.txt >train_neg_full_pp.txt (example if file to preprocess is in the same folder as script)

2 run preprocess2.py just change name variable to be the file you want to preprocess2.
3 folder preprocess already includes preprocessed files
preprocess of standford pretrained embeddings:
run from_glove2pickle.py just select the text file you are interested in.